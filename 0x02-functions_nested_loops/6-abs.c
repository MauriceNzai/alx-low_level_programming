#include "main.h"

/**
 * _abs - See description.
 *
 * @r: An integer input
 *
 * Description: Computes the absolute value of an integer.
 *
 * Return: Absolute value of r
*/

int _abs(int r)
{
if (r >= 0)
return (r);
else
return (r * -1);
}
