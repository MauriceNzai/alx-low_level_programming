#include "main.h"

/**
 * _abs - See description.
 *
 * @x: An integer input
 *
 * Description: Computes the absolute value of an integer.
 *
 * Return: Absolute value of x
*/

int _abs(int x)
{
if (x >= 0)
return (x);
else
return (r * -1);
}
